apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base/mall-mysql.yaml
  - base/mall-db-client.yaml
  - base/shuailli-cert.yaml
  - base/mall-router.yaml

namespace: mall
