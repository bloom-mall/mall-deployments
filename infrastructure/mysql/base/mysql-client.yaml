apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-client
  template:
    metadata:
      labels:
        app: mysql-client
    spec:
      containers:
        - name: mysql-client
          image: adminer:5.3.0
          ports:
            - containerPort: 8080
          env:
            - name: ADMINER_DEFAULT_SERVER
              value: mysql
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-client
spec:
  selector:
    app: mysql-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
